import os
import sys

# Add the project root to the python path so that 'submission67' can be imported
# or 'submission' if we rename it.
sys.path.insert(0, os.path.abspath(os.path.join(os.path.dirname(__file__), '..')))
